{
  "name": "risein-midnight-dapp",
  "version": "1.0.0",
  "private": true,
  "description": "Rise In Level 2 — Midnight Network DApp with Lace wallet, Compact contract, and ZK proofs on Preprod",
  "workspaces": [
    "contract",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "preview": "npm run preview --workspace=frontend",
    "compile-contract": "npm run compile --workspace=contract"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
